# 系统架构文档

## 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                     用户浏览器                           │
│  ┌──────────────────────────────────────────────────┐  │
│  │         React 前端应用 (Port 3000)                │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │  │
│  │  │ 菜单栏   │  │ 状态栏   │  │  3D展示区     │   │  │
│  │  └──────────┘  └──────────┘  └──────────────┘   │  │
│  │  ┌────────────────────────────────────────────┐  │  │
│  │  │           功能区（动态切换）                │  │  │
│  │  └────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                        │
                        │ HTTP / WebSocket
                        │
┌─────────────────────────────────────────────────────────┐
│                    Nginx (Port 80)                      │
│  ┌──────────────────┐  ┌──────────────────────────┐    │
│  │  静态文件服务     │  │   反向代理               │    │
│  │  (React Build)   │  │  /api/* → C++ Backend   │    │
│  │                  │  │  /ws → C++ WebSocket    │    │
│  └──────────────────┘  └──────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
                        │
                        │
┌─────────────────────────────────────────────────────────┐
│          C++ 后端服务 (Drogon, Port 8080)               │
│  ┌──────────────────┐  ┌──────────────────────────┐    │
│  │   REST API       │  │   WebSocket Server       │    │
│  │   - /api/v1/*    │  │   - /ws                  │    │
│  └──────────────────┘  └──────────────────────────┘    │
│  ┌──────────────────────────────────────────────────┐  │
│  │           机器人状态管理 (线程安全)                │  │
│  │   - 基础状态更新                                       │  │
│  │   - 命令处理                                       │  │
│  │   - 电机状态更新                                       │  │
│  │   - 多客户端广播                                   │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

## 技术栈

### 前端
- **React 19.2.3** - UI 框架
- **React Router 6.21.1** - 路由管理
- **Three.js 0.158.0** - 3D 图形库
- **@react-three/fiber 8.15.11** - React Three.js 绑定
- **@react-three/drei 9.88.13** - Three.js 工具库

### 后端
- **C++17** - 编程语言
- **Drogon** - Web 框架（REST API + WebSocket）
- **jsoncpp** - JSON 解析库 版本：1.9.5

### 部署
- **Nginx 1.18+** - Web 服务器和反向代理

## 数据流

### 状态更新流程
```
机器人硬件/模拟器 → C++ 后端状态管理 → WebSocket 广播 → React 前端 → 更新 UI
```

### 控制命令流程
```
用户操作(前端页面) → React WebSocket → C++ 后端处理 → 更新状态 → 广播更新 → 前端显示
```

### REST API 流程
```
前端组件 → HTTP 请求 → Nginx 反向代理 → C++ 后端 API → JSON 响应 → 前端更新数据
```

## 核心组件

### 前端
- **App.js** - 主应用，WebSocket 连接管理，管理全局状态，路由配置
- **Sidebar.js** - 左侧导航菜单，路由切换，菜单项高亮
- **StatusBar.js** - 顶部状态栏（显示连接状态、电池、电压），实时更新
- **Robot3DView.js** - 3D 机器人模型展示，Three.js 场景渲染，关节交互控制，模型旋转、缩放、平移
- **功能页面** 
  - SensorData.js：传感器数据展示
  - RobotControl.js：机器人控制面板
  - LogViewer.js：日志查看器
  - SystemInfo.js：系统信息展示

### 后端
- **main.cpp** - 主程序入口，Drogon 路由配置
- **robot_state.h/cpp** - 机器人状态管理（线程安全）
- **api_handlers.h/cpp** - REST API 处理器
- **websocket_handler.h/cpp** - WebSocket 处理器

## 消息协议

### WebSocket 消息格式

#### 客户端 → 服务器

**控制命令:**
```json
{
  "type": "command",
  "command_type": "start|stop|reset|emergency_stop",
  "data": {},
  "timestamp": 1234567890
}
```

**下载日志:**
```json
{
  "type": "set_joint_angle",
  "data": {
    "joint_id": "joint1",
    "angle": 45.0
  },
  "timestamp": 1234567890
}
```

#### 服务器 → 客户端

**状态更新:**
```json
{
  "type": "robot_state",
  "data": {
    "battery": 100,
    "voltage": 24.0,
    "status": "running",
    "mode": "normal",
    "joints": [
   ,   {"id": "joint1", "name": "关节1", "angle": 45.0}
    ]
    "position": {"x": 1.0, "y": 2.0, "theta": 0.5}
  },
  "timestamp": 1234567890
}
```

**命令确认:**
```json
{
  "type": "ack",
  "data": {"message": "Command received"},
  "timestamp": 1234567890
}
```

## API 接口

### REST API
- `GET /api/v1/status` - 获取机器人状态

### WebSocket
- `ws://localhost:8080/ws` - WebSocket 连接端点

**消息格式**详见 [后端 README](backend/README.md#api-端点)

## 环境部署
详见 [x86环境构建]

## nx部署
确保nx的BSP环境支持，否则无法运行，部署方式详见 [nx环境部署](nginx/NX_DEPLOYMENT_GUIDE.md)

## 扩展方向

- **认证授权** - JWT Token，WebSocket 连接验证
- **数据持久化** - 数据库存储历史数据
- **ROS2 集成** - 订阅/发布 ROS2 话题
- **性能优化** - 前端代码分割，WebSocket 消息压缩
- **功能扩展** - 多机器人支持，实时视频流，地图显示

## 安全考虑

1. **HTTPS** - 生产环境使用 HTTPS
2. **CORS** - 限制允许的源
3. **输入验证** - 验证所有用户输入
4. **速率限制** - API 请求频率限制
5. **认证授权** - 实现用户认证和权限控制
